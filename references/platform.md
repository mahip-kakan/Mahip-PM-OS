# Platform Setup — [Your Name]

*Populated by `/onboard`. Tells Claude how you access PM-OS and what's wired.*

---

## Primary interface

- [ ] **Claude Code** (terminal — `claude` in this folder)
- [ ] **Claude Cowork** (desktop — folder synced or opened as workspace)
- [ ] **Cursor** (IDE — `.cursor/skills/` + rules from `CLAUDE.md`)
- [ ] **Other:** [FILL IN]

---

## Cowork-specific notes

*If using Claude Cowork:*

1. Open this folder as your Cowork workspace (clone locally or sync via cloud drive).
2. Skills in `.claude/skills/` load automatically when relevant.
3. Commands: type `/discover`, `/red-team-prd`, `/meeting-notes`, `/analyze-test`.
4. Wire MCPs in Cowork settings (Jira, Gmail, analytics DB, etc.) — then update `connections.md`.

**Folder path on this machine:** [FILL IN — e.g. ~/Mahip-PM-OS]

---

## MCP connections (summary)

See `connections.md` for the full registry. Status at onboarding:

| Tool | Wired? | Notes |
|------|--------|-------|
| [FILL IN] | planned / active | |

---

## Safety preferences

- **Email:** Draft only — never send without explicit approval
- **Jira / tasks:** Read by default — create/update only when asked
- **Analytics DB:** Read-only queries only
- **Other:** [FILL IN]
